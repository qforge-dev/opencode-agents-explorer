{
  "name": "@qforge/opencode-agents-explorer",
  "version": "0.1.6",
  "description": "OpenCode plugin that automatically injects AGENTS.md files from sub folders when an agent reads files",
  "private": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --format esm --target node --dts",
    "prepublishOnly": "bun run build",
    "release": "npm version patch && npm publish && git push --follow-tags",
    "test": "bun test"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@opencode-ai/plugin": "^1.1.42"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
